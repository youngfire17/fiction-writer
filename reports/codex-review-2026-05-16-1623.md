[P0] skill/SKILL.md:116-125 ? `revise-to-target` reads `manuscript.md` before `export` creates it, so the revision phase can start from a missing file. Fix: make revise read `chapters/chapter-*.md` or run a defined stitch step before revision.

[P0] skill/SKILL.md:123-125 ? `export` requires a fiction-typeset pandoc template but no path or template file is produced in this scaffold. Fix: add the template file and reference its exact path, or remove the template flag from the export contract.

[P1] subagents/plotter.md:34-37 ? Plotter writes a 5-column `setup-payoff.md`, while foreshadowing-tracker and the indexer expect the 7-column schema. Fix: standardize all producers/consumers on `| id | description | type | planted | payoff | status | notes |`.

[P1] tools/build_continuity_index.py:171-173 ? The indexer silently skips plotter-produced setup/payoff rows because it requires at least 6 cells after `id`. Fix: align parser with the canonical table schema and fail loudly when rows are malformed.

[P1] subagents/drafter.md:57-70 ? Drafter footers omit `characters-present` and `in-world-time`, but the indexer only populates those event fields from those exact footer keys. Fix: add required footer keys to drafter output and acceptance checks.

[P1] tools/build_continuity_index.py:198-200 ? Event parsing depends on exact footer keys that are not declared in the chapter-brief template or drafter output. Fix: define a canonical chapter footer schema in one place and have drafter, editors, and indexer use it.

[P1] subagents/drafter.md:24 ? Drafter looks for `series-bible/characters/<pov>.md`, but character-designer writes `<slug-name>.md`, so POV names with spaces/case will miss files. Fix: require `cast-index.md` to include `file` paths and have drafter resolve sheets through it.

[P1] subagents/plotter.md:13 ? Plotter reads `series-bible/*.md`, which misses the actual nested `series-bible/world/` and `series-bible/characters/` canon. Fix: change input to `series-bible/**/*.md` or explicitly list world/character folders.

[P1] tools/build_continuity_index.py:69-74 ? `world_rules` is created but never populated, while continuity-checker is told to query it for world-rule checks. Fix: parse `world/*.md` into `world_rules` or remove that table from the checker contract.

[P1] subagents/continuity-checker.md:3-7 ? Frontmatter says continuity-checker writes only notes and SQLite, but the process tells it to edit character and world bible files. Fix: update the description/tools contract or move bible updates to the owning agents.

[P1] subagents/drafter.md:61-75 ? Drafter is both forbidden to invent world facts and told to include new world facts it used in a footer for later canonization. Fix: make new world facts blocking TODOs resolved by world-builder before prose handoff.

[P1] subagents/continuity-checker.md:26 ? Continuity-checker appends drafter-flagged world facts to world files, contradicting world-builder ownership of canonical world facts. Fix: have continuity-checker write a pending-canon report and route world changes to world-builder.

[P1] README.md:7 ? README says the next test book is `books/the-cartographer/`, while the locked decision and next steps use `books/the-forgetting-wing/`. Fix: update the stale project slug to `books/the-forgetting-wing/`.

[P2] skill/SKILL.md:108-110 ? Continuity failure loops back to drafter with no retry limit, escalation rule, or rollback point. Fix: add max attempts, preserve failed draft snapshots, and escalate after repeated fail reports.

[P2] tools/build_continuity_index.py:110 ? UTF-8 BOM files can cause the first `#` heading not to match, silently skipping a character sheet on Windows. Fix: read markdown with `encoding="utf-8-sig"`.

[P2] skill/SKILL.md:146-156 ? Resume logic infers state from existing chapter files only, with no phase log or last-success marker. Fix: add `books/<slug>/run-state.md` recording phase, inputs, outputs, status, and last completed chapter.